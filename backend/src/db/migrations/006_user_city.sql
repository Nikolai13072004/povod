-- Город пользователя для профиля (BE-010).
-- Раньше город показывался только из VK и нигде не сохранялся, поэтому
-- профиль расходился с сервером и терялся вне VK.
ALTER TABLE users ADD COLUMN IF NOT EXISTS city TEXT;
