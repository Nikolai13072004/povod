# Dependencies and builds
node_modules/
dist/

# Local environment and storage
.env
.env.*
!.env.example
.env.local
# Только снимок in-memory хранилища бэкенда. Без ведущего слэша шаблон `data/`
# матчил ЛЮБОЙ каталог с таким именем на любой глубине — так исходники
# frontend/src/data молча не попадали в коммит, и сборка падала уже в CI.
/backend/data/

# Logs and operating system files
*.log
.DS_Store
Thumbs.db

# Локальные конфиги инструментов
.claude/
docker_run_err.txt
