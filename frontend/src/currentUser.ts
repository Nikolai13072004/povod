import type { User } from "./services/api";

/**
 * Пустой профиль до подтверждения сессии сервером.
 *
 * Здесь лежал сид-пользователь бэкенда — с именем «Эльмира Гильманова», её
 * почтой и аватаркой со стороннего сервиса. На первом кадре, пока не ответил
 * `/api/Auth/session`, приложение показывало этого человека всем подряд: в
 * шапке мелькало чужое имя и чужое фото. На локальной разработке это выглядело
 * деталью оформления, на проде — чужими данными на экране.
 *
 * Заглушка обязана быть заведомо ничьей. Пустое имя даёт нейтральную аватарку
 * вместо чужого лица, а пустой `id` не совпадает ни с одним настоящим — значит
 * проверки вида «это моё событие?» не срабатывают ложно на первом кадре.
 */
export const CURRENT_USER: User = {
  id: "",
  name: "",
  createdAt: "1970-01-01T00:00:00.000Z",
};
