import styled from "@emotion/styled";

/**
 * Единый адаптивный контейнер контента POVOD.
 *
 * Центрирует колонку и ограничивает её максимальную ширину, не добавляя
 * собственных горизонтальных отступов: на мобильных экранах контент остаётся
 * во всю доступную ширину, а внутренние отступы задают сами страницы и chrome.
 *
 * Максимальная ширина берётся из CSS-переменной `--povod-content-max`, которую
 * оболочка приложения задаёт в зависимости от маршрута (узкая для авторизации и
 * онбординга, широкая для списков с сеткой карточек).
 */
export const ContentWidth = styled.div`
  width: 100%;
  max-width: var(--povod-content-max, 760px);
  margin-inline: auto;
  box-sizing: border-box;
`;
